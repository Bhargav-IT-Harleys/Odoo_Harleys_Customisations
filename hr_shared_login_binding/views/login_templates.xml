<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="custom_login" name="HR Shared Login">
        <t t-call="web.login_layout">
            <t t-set="disable_footer">1</t>
            <div class="oe_login_form">
                <h5 class="card-title">Welcome</h5>
                <p class="text-muted">Please sign in to continue</p>
                <t t-if="error">
                    <div class="alert alert-danger" role="alert">
                        <t t-esc="error"/>
                    </div>
                </t>
                <form method="POST" action="/hr_shared_login_binding/login">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <t t-if="redirect">
                        <input type="hidden" name="redirect" t-att-value="redirect"/>
                    </t>
                    <div class="mb-3">
                        <label class="fw-bold" for="login">Email</label>
                        <input id="login" name="login" type="text"
                               class="form-control mb-2" autocomplete="username"
                               autofocus="autofocus" required="required"/>
                    </div>
                    <div class="mb-3">
                        <label class="fw-bold" for="password">Password</label>
                        <div class="input-group mb-2">
                            <input id="password" name="password" type="password"
                                   class="form-control" autocomplete="current-password"
                                   required="required"/>
                            <button type="button" class="btn btn-sm border border-start-0 o_show_password">
                                <i class="fa fa-eye"/>
                            </button>
                        </div>
                    </div>
                    <div class="clearfix oe_login_buttons text-center d-grid mb-1">
                        <button type="submit" class="btn btn-primary">Sign In</button>
                    </div>
                </form>
            </div>
        </t>
    </template>
</odoo>
